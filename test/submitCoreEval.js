const main = () => {
  /* 
    get the path to the following:
        - core-eval.js
        - proposal-permit.json
    get the name of key 
    */
  const permit = "";
  const coreEval = "";
  const key = "";
  const deposit = "1000000ubld";
  const chainID = "agoric-mainfork-1";

  submitCoreEval(permit, coreEval, deposit, chainID, key);
};

main();
